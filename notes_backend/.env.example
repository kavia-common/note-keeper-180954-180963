# Flask app configuration
FLASK_ENV=production
FLASK_DEBUG=0
PORT=3001

# Notes backend selection (future use)
NOTES_BACKEND=memory  # values: memory, db

# Database connection settings (for future db-backed implementation)
NOTES_DB_URL=
NOTES_DB_USER=
NOTES_DB_PASSWORD=
NOTES_DB_NAME=
NOTES_DB_PORT=
